---
name: N0ctyra 仓库写入范围限制
description: 在 SiyuWei1/N0ctyra 仓库中，Claude 仅可写入 MacClaude/ 目录，其他目录只读
type: feedback
originSessionId: fbd8f6f3-9043-478b-8ad7-c3805081202e
---
在 `SiyuWei1/N0ctyra` 仓库（本地路径：`/Users/n0cytra/OneDrive - The Pennsylvania State University/SIyu W/N0ctyra`）中：

- **可写范围**：仅 `MacClaude/` 目录下。可在其中自由创建子文件夹、做分类组织。
- **只读范围**：仓库内其他所有目录和文件——可以读取作为参考，但不能修改、删除、移动。

**Why:** 用户明确划定了权限边界，MacClaude 是分配给 Claude 的专属工作区。

**How to apply:** 在该仓库做任何写操作前，确认目标路径在 `MacClaude/` 下。需要修改仓库其他位置时，先告知用户并请求许可，不要擅自改动。
